package Code_Part;
public class Obstacle extends Maps {
    // Method to kill the player
    public void kill(Player player) {
        // Implementation details for killing the player.
    }

    // Method to check if collision occurs when moving upside
    public boolean moveUpsideCollide() {
        // Implementation details for checking collision when moving upside.
        return false;
    }

    // Method to check if collision occurs when moving downside
    private boolean moveDownsideCollide() {
        // Implementation details for checking collision when moving downside.
        return false;
    }
}
